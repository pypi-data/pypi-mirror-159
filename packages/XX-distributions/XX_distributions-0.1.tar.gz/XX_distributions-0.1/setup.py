from setuptools import setup

setup(name='XX_distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['XX_distributions'],
      author='Xi Xi',
      zip_safe=False)
