var expect = require("chai").expect;
var pwcHelloWorld = require("../index.js");

describe("Simple Test", () => {
    before(() => {
        console.log(" -- Initiating unit test run...");
    });

    after(() => {
        console.log(" -- Unit Testing complete...");
    });

    describe("Test Group 1", () => {
        beforeEach(() => {
            console.log(`Testing pwc-hello-world function...`);
        });

        it(`... returns "Hello, World!"`, () => {
            const helloWorld = pwcHelloWorld();
            expect(helloWorld).to.equal("Hello, World!");
        });
    });
});
