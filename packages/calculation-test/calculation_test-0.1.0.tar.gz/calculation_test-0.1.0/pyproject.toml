[tool.poetry]
name = "calculation_test"
version = "0.1.0"
description = "mapilio calculation"
readme = "README.rst"
authors = ["Kadir Coşar <kadircosarnew@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.10"
addict = "2.4.0"
geographiclib = "1.50"
numpy = "^1.23.1"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
