from setuptools import setup

setup(name='Distributions_GauBino',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['Distributions_GauBino'],
      zip_safe=False)
