[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "whirly"
authors = [{name = "Dave Connelly", email = "dsconnelly@nyu.edu"}]
readme = "README.md"
license = {file = "LICENSE"}
classifiers = ["License :: OSI Approved :: MIT License"]
dynamic = ["version", "description"]
dependencies = [
    "matplotlib",
    "numpy"
]

[project.urls]
Home = "https://github.com/dsconnelly/whirly"
