[tool.poetry]
name = "mad-monkey"
version = "0.2.0"
description = ""
authors = ["Aadityansha"]
readme = "README.md"

[tool.poetry.scripts]
monkey = "mad_monkey.main:app"

[tool.poetry.dependencies]
python = "^3.10"
typer = {extras = ["all"], version = "^0.6.1"}

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
