[tool.poetry]
name = "isprime_pypackage"
version = "0.1.0"
description = "A python pacakge to check if a number is prime"
authors = ["Arslan909 <Arslan909@gmail.com>"]
license = "WTFPL"

[tool.poetry.dependencies]
python = "^3.10"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
isprime_pyPackage = "isprime_pypackage:isPrime"