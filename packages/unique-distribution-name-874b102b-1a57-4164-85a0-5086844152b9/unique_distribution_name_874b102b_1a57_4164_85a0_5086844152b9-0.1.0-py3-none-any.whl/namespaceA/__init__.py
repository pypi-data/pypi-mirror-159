from namespaceA import *

# just like the `__init__()` method is ran as soon as an object is initialized, so too is the `__init__.py` module ran as soon as a package is imported

# setup code that should be run even before main entry point in __main__.py
# immediately ran on import of the package and on running the package by passing it as argument to the python interpreter
# https://youtu.be/cONc0NcKE7s
