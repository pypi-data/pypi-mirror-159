# Tutorial

- [Introduce](#introduce)

## Introduce

this is tool modules.

```bash
$ dvs-vsws.exe -h
usage: dvs-vsws [-h] [-V] [-el [exclude-list [exclude-list ...]]] [destination-directory [destination-directory ...]]

    this is a dvs android execution program.
        

positional arguments:
  destination-directory
                        the current destination directories to be searched.

optional arguments:
  -h, --help            show this help message and exit
  -V, --version         the show version and exit.
  -el [exclude-list [exclude-list ...]], --exclude-list [exclude-list [exclude-list ...]]
                        the collection of paths excluded by the current search index.

the copyright belongs to DovSnier that reserve the right of final interpretation.
```
